% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_alignment.R
\name{lift_aligned}
\alias{lift_aligned}
\title{Lift Aligned Data Into an Ambient Feature Space}
\usage{
lift_aligned(
  x,
  to = c("reference", "subject", "each"),
  subject = NULL,
  aligned = NULL,
  inverse = c("pinv", "ridge", "transpose", "solve", "auto"),
  tol = sqrt(.Machine$double.eps),
  lambda = 1e-06
)
}
\arguments{
\item{x}{An \code{\link{AlignmentResult}} (preferred) or \code{\link{AlignmentModel}}.}

\item{to}{Target ambient space to lift into:
\itemize{
\item \code{"reference"}: use the model's reference subject space.
\item \code{"subject"}: use a specific \code{subject}.
\item \code{"each"}: lift each aligned matrix into its own subject space.
}}

\item{subject}{Subject ID used when \code{to="subject"}.}

\item{aligned}{Optional aligned data to lift. If \code{x} is an
\code{AlignmentResult} and \code{aligned} is \code{NULL}, uses
\code{\link{get_aligned}(x)}.
Can be a single matrix or a named list of matrices.}

\item{inverse}{How to compute the inverse operator; forwarded to
\code{\link{inverse_transform}}. Use \code{"pinv"} or \code{"ridge"} for
bottleneck (non-square) operators.}

\item{tol}{Tolerance for \code{inverse="pinv"} (relative to largest singular value).}

\item{lambda}{Regularization strength for \code{inverse="ridge"}.}
}
\value{
A matrix (if \code{aligned} is a matrix) or a named list of matrices.
}
\description{
Many aligners (e.g., manifoldalign projection methods) map high-dimensional
feature data \verb{X (p x n)} into a shared bottleneck space \verb{Z (k x n)} using
non-square operators \verb{A (k x p)}. This helper maps aligned data back into an
ambient feature space using an approximate inverse (pseudoinverse or ridge).
}
\details{
Typical uses:
\itemize{
\item Lift aligned latent data into the reference subject's voxel space.
\item "Paint" a shared latent representation into a particular subject's voxel
space for visualization or decoding.
}
}
