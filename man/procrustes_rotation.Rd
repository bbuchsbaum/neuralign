% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aligner_procrustes.R
\name{procrustes_rotation}
\alias{procrustes_rotation}
\title{Procrustes Rotation (Convention-Safe)}
\usage{
procrustes_rotation(
  source,
  target,
  convention = c("left", "right"),
  scale = FALSE,
  reflection = FALSE,
  obs_labels_source = NULL,
  obs_labels_target = NULL,
  min_overlap = 2L,
  allow_reflection = NULL,
  rank = NULL
)
}
\arguments{
\item{source}{Source matrix.}

\item{target}{Target/reference matrix.}

\item{convention}{Multiplication convention (\code{"left"} or \code{"right"}).}

\item{scale}{Logical; if \code{TRUE}, include optimal scale factor.}

\item{reflection}{Logical; if \code{TRUE}, allow reflections (det(Q) may be -1).}

\item{obs_labels_source}{Optional observation labels for \code{source}.}

\item{obs_labels_target}{Optional observation labels for \code{target}.}

\item{min_overlap}{Minimum number of shared labels when labels are supplied.}

\item{allow_reflection}{Alias for \code{reflection}.}

\item{rank}{Optional truncated-SVD rank for Procrustes fitting. If \code{NULL}
(default), uses the full SVD. If provided, must be a positive integer
\verb{<= min(n_features, n_obs)}.}
}
\value{
A list with elements \code{Q}, \code{scale_factor}, \code{residual},
\code{convention}, and \code{matched_labels}.
}
\description{
Compute the (optionally scaled) Procrustes operator aligning a source matrix
to a target matrix, with explicit left/right multiplication conventions.
}
\details{
\itemize{
\item \code{convention="left"} solves for \code{Q} such that \verb{Q \%*\% source ≈ target}
where matrices are \verb{(features x observations)}.
\item \code{convention="right"} solves for \code{Q} such that \verb{source \%*\% Q ≈ target}
where matrices are \verb{(observations x features)}.
}

When per-matrix observation labels are supplied, alignment is computed on
the intersection of labels (in the order they appear in \code{obs_labels_source}).
}
\examples{
set.seed(1)
Q_true <- qr.Q(qr(matrix(rnorm(9), 3, 3)))
if (det(Q_true) < 0) Q_true[, 1] <- -Q_true[, 1]
X <- matrix(rnorm(15), 3, 5)
Y <- Q_true \%*\% X

fit <- procrustes_rotation(X, Y, convention = "left")
max(abs(fit$Q - Q_true)) < 1e-6

}
\seealso{
Other procrustes: 
\code{\link{procrustes_distance}()}
}
\concept{procrustes}
