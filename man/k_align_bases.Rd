% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/k_procrustes.R
\name{k_align_bases}
\alias{k_align_bases}
\title{Align Multiple Bases to a Reference}
\usage{
k_align_bases(
  U_list,
  K,
  ref = 1L,
  allow_reflection = TRUE,
  weights = NULL,
  reflection = NULL
)
}
\arguments{
\item{U_list}{List of q x r bases.}

\item{K}{Square kernel matrix.}

\item{ref}{Reference basis or index (default 1).}

\item{allow_reflection}{Passed to \code{k_procrustes()}.}

\item{weights}{Optional numeric weights (stored in return value).}

\item{reflection}{Alias for \code{allow_reflection}.}
}
\value{
A list with elements \code{U_aligned}, \code{R}, \code{Uref}, \code{score}, \code{weights}.
}
\description{
Convenience wrapper aligning a list of bases to a single reference (either a
matrix or an index into \code{U_list}).
}
\examples{
set.seed(1)
q <- 5
r <- 2
K <- diag(q)
U_list <- lapply(1:3, function(i) qr.Q(qr(matrix(rnorm(q * r), q, r))))
out <- k_align_bases(U_list, K, ref = 1)
length(out$U_aligned)
}
\seealso{
Other k_procrustes:
\code{\link{k_consensus_basis}()},
\code{\link{k_kernel_roots}()},
\code{\link{k_orthonormalize}()},
\code{\link{k_procrustes}()}
}
\concept{k_procrustes}
