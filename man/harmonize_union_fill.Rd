% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/harmonize.R
\name{harmonize_union_fill}
\alias{harmonize_union_fill}
\title{Harmonize Matrices to a Union of IDs}
\usage{
harmonize_union_fill(
  mats,
  ids = NULL,
  axis = c("rows", "cols"),
  union_ids = NULL,
  union_order = c("first_seen", "sorted"),
  fill = 0,
  min_coverage = 1L,
  warn_sparse_below = 0.5
)
}
\arguments{
\item{mats}{Named list of matrices (or \code{Matrix} objects).}

\item{ids}{Optional named list of ID vectors corresponding to the chosen
axis for each matrix. If \code{NULL}, IDs are inferred from \code{rownames()} or
\code{colnames()} depending on \code{axis}.}

\item{axis}{Which margin to harmonize: \code{"rows"} or \code{"cols"}.}

\item{union_ids}{Optional character vector specifying the union vocabulary.
If provided, order is used as-is (and \code{union_order} is ignored).}

\item{union_order}{Ordering used when computing a union (ignored when
\code{union_ids} is provided): \code{"first_seen"} preserves first appearance order;
\code{"sorted"} sorts IDs.}

\item{fill}{Fill value for missing entries (default 0).}

\item{min_coverage}{Minimum number of observed IDs required per subject. Any
subject with fewer observed IDs is dropped.}

\item{warn_sparse_below}{Warn when a subject observes fewer than this
fraction of union IDs (after filtering).}
}
\value{
A list with elements:
\describe{
\item{mats}{Named list of harmonized matrices.}
\item{ids}{Character vector of union IDs.}
\item{obs_mask}{Named list of logical vectors (TRUE = originally present).}
\item{pair_counts}{Integer matrix of co-observation counts for ID pairs.}
\item{coverage}{Data frame with columns \code{id} and \code{n_subjects}.}
\item{dropped}{Character vector of dropped subject IDs.}
}
}
\description{
Harmonize a named list of matrices to a shared union of row/column IDs,
filling missing entries with \code{fill} (default 0). This is useful when subjects
have partially overlapping feature/effect vocabularies but a downstream
method expects all matrices to live in a common space.
}
\details{
The function also returns provenance metadata describing which IDs were
observed in each subject, along with pairwise co-observation counts.
}
\examples{
x1 <- matrix(1:6, 3, 2, dimnames = list(c("a", "b", "c"), c("v1", "v2")))
x2 <- matrix(1:4, 2, 2, dimnames = list(c("b", "d"), c("v1", "v2")))
res <- harmonize_union_fill(list(s1 = x1, s2 = x2), axis = "rows", fill = 0)
res$ids
rownames(res$mats$s1)
rownames(res$mats$s2)

}
\concept{harmonize}
