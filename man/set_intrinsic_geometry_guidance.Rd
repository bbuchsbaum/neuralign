% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intrinsic_geometry.R
\name{set_intrinsic_geometry_guidance}
\alias{set_intrinsic_geometry_guidance}
\title{Add Intrinsic Geometry Guidance to AlignmentData}
\usage{
set_intrinsic_geometry_guidance(
  data,
  adjacency_by_subject,
  channel_name = "intrinsic",
  k = 10,
  normalized = TRUE,
  drop_zero = TRUE,
  tol = 1e-10,
  method = c("auto", "eigen", "rspectra"),
  validate = TRUE
)
}
\arguments{
\item{data}{An \code{AlignmentData} object.}

\item{adjacency_by_subject}{Named list of symmetric adjacency matrices, one
per subject.}

\item{channel_name}{Guidance channel name (default \code{"intrinsic"}).}

\item{k}{Number of eigenmodes to compute.}

\item{normalized}{Logical; use normalized Laplacian.}

\item{drop_zero}{Logical; drop near-zero eigenmodes.}

\item{tol}{Tolerance for zero eigenvalues.}

\item{method}{Eigen solver (see \code{laplacian_eigenmodes()}).}

\item{validate}{Logical; if \code{TRUE}, validate dimensional compatibility.}
}
\value{
The modified \code{AlignmentData}.
}
\description{
Compute Laplacian eigenmodes per subject and store them as \code{"coords"} guidance
channels on an \code{AlignmentData} object.
}
\examples{
set.seed(1)
# Line-graph adjacency for 5 nodes
A <- matrix(0, 5, 5)
A[cbind(1:4, 2:5)] <- 1
A[cbind(2:5, 1:4)] <- 1
adat <- AlignmentData(list(
  s1 = matrix(rnorm(20), 5, 4),
  s2 = matrix(rnorm(20), 5, 4)
))
adat2 <- set_intrinsic_geometry_guidance(adat, list(s1 = A, s2 = A), k = 2, method = "eigen")
Z1 <- get_guidance(adat2, subject = "s1")$intrinsic$value
dim(Z1)
}
