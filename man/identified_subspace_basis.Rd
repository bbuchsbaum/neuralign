% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subspace.R
\name{identified_subspace_basis}
\alias{identified_subspace_basis}
\title{Compute an Identified Subspace Basis}
\usage{
identified_subspace_basis(
  x,
  convention = c("right", "left"),
  tol = sqrt(.Machine$double.eps)
)
}
\arguments{
\item{x}{Numeric matrix (correspondence features x transform dimension, or
vice versa depending on convention). Must be finite.}

\item{convention}{Convention for interpreting the transform dimension:
\code{"right"} means transforms act on columns; \code{"left"} means transforms act on
rows.}

\item{tol}{Relative tolerance for numeric rank determination (fraction of
the largest singular value).}
}
\value{
An object of class \code{"identified_subspace"} with components:
\describe{
\item{basis}{\verb{d x r} orthonormal basis matrix (columns).}
\item{singular_values}{Singular values of \code{x}.}
\item{numeric_rank}{Computed numeric rank \code{r}.}
\item{transform_dim}{Transform dimension \code{d}.}
\item{convention}{The convention used.}
\item{tol}{The tolerance used.}
}
}
\description{
Given a correspondence matrix that drives alignment, compute an orthonormal
basis for the identified subspace of the transform space.
}
\details{
If \code{convention="right"} (transforms act on columns), the identified subspace
is the span of the right singular vectors corresponding to non-zero singular
values. If \code{convention="left"} (transforms act on rows), the identified
subspace is the span of the corresponding left singular vectors.
}
\examples{
set.seed(1)
X <- matrix(rnorm(12), 3, 4)
sb <- identified_subspace_basis(X, convention = "right")
dim(sb$basis)

}
