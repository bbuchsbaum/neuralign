% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subspace.R
\name{canonicalize_orthogonal_operator}
\alias{canonicalize_orthogonal_operator}
\title{Canonicalize an Orthogonal Operator in the Identified Subspace}
\usage{
canonicalize_orthogonal_operator(
  Q,
  x,
  convention = c("left", "right"),
  tol = sqrt(.Machine$double.eps),
  check_orthogonal = TRUE,
  orthogonal_tol = 1e-06
)
}
\arguments{
\item{Q}{Square orthogonal operator (matrix or Matrix).}

\item{x}{Correspondence matrix used to determine the identified subspace.}

\item{convention}{Convention for interpreting the transform dimension:
\code{"left"} means transforms act on rows (\code{d = nrow(x)}); \code{"right"} means
transforms act on columns (\code{d = ncol(x)}).}

\item{tol}{Relative tolerance for numeric rank determination (fraction of the
largest singular value).}

\item{check_orthogonal}{Logical; if TRUE (default), check that \code{Q} is
orthogonal (within \code{orthogonal_tol}) before canonicalizing.}

\item{orthogonal_tol}{Tolerance for the orthogonality check.}
}
\value{
A \verb{d x d} orthogonal matrix.
}
\description{
When a correspondence matrix is rank-deficient, orthogonal operators are
under-identified: their action on the orthogonal complement of the identified
subspace does not affect the fit. This helper replaces that arbitrary part
with a deterministic extension, while preserving the action of \code{Q} on the
identified subspace of \code{x}.
}
\details{
In particular, if \code{S} is the identified subspace of the transform space
implied by \code{x}, the returned operator \code{Q_canon} satisfies:
\deqn{Q_{\mathrm{canon}} v = Q v \quad \forall v \in S.}
}
\examples{
set.seed(1)
X <- matrix(rnorm(9), 3, 3)
X[, 3] <- X[, 2]  # rank-deficient (right-convention transform_dim = ncol(X))
Q <- diag(3)
Qc <- canonicalize_orthogonal_operator(Q, X, convention = "right")
dim(Qc)
}
