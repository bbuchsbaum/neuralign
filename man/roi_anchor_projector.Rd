% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/roi_anchors.R
\name{roi_anchor_projector}
\alias{roi_anchor_projector}
\title{ROI Anchor Projector}
\usage{
roi_anchor_projector(
  roi,
  anchors = NULL,
  n_features = NULL,
  normalize = TRUE,
  sparse = TRUE
)
}
\arguments{
\item{roi}{ROI specification (see details).}

\item{anchors}{Optional character vector of anchor names to include and order.}

\item{n_features}{Optional feature count (useful for list-of-indices input).}

\item{normalize}{Logical; if \code{TRUE}, normalize each anchor row to sum to 1.}

\item{sparse}{Logical; if \code{TRUE}, return a sparse \code{Matrix} when possible.}
}
\value{
An (anchors x features) projector matrix/Matrix.
}
\description{
Construct an (anchors x features) projector \code{P} for a single subject.
If \code{X} is a subject data matrix \verb{(features x observations)}, then
\code{P \%*\% X} yields \verb{(anchors x observations)} anchor summaries.
}
\details{
\code{roi} can be:
\itemize{
\item an atomic vector of length \code{n_features} giving an ROI label per feature
\item a named list mapping ROI names to integer feature indices
\item an (anchors x features) weight matrix (used as-is, optionally normalized)
}
}
\examples{
# ROI labels per feature (length == n_features)
roi <- c("V1", "V1", "MT", "MT", "MT")
P <- roi_anchor_projector(roi, normalize = TRUE, sparse = FALSE)
dim(P)
}
\seealso{
Other guidance:
\code{\link{get_guidance}()},
\code{\link{guidance_channel}()},
\code{\link{intrinsic_geometry}},
\code{\link{laplacian_eigenmodes}()},
\code{\link{roi_anchor_projectors}()},
\code{\link{roi_anchors}},
\code{\link{set_guidance}()}
}
\concept{guidance}
