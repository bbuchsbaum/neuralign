% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv.R
\name{create_obs_folds}
\alias{create_obs_folds}
\title{Create Observation-Axis Fold Specifications}
\usage{
create_obs_folds(
  obs_ids,
  method = c("run", "blocked_time"),
  k = 5,
  guard_tr = 0L,
  seed = NULL,
  id_policy = c("intersection", "union")
)
}
\arguments{
\item{obs_ids}{Observation identifiers. Either a single vector (shared across
subjects), or a named list of per-subject vectors.}

\item{method}{Fold construction method:
\itemize{
\item \code{"run"}: leave-one-run-out (each unique run id defines a fold)
\item \code{"blocked_time"}: split a single time series into contiguous blocks
}}

\item{k}{Number of blocks for \code{method="blocked_time"}.}

\item{guard_tr}{Number of TRs to exclude around each held-out block/run from
the training set (to reduce temporal leakage).}

\item{seed}{Optional random seed (currently only used for future extensions).}

\item{id_policy}{For per-subject \code{obs_ids} lists and \code{method="run"}, choose how
fold ids are defined: \code{"intersection"} uses only run ids present for all
subjects; \code{"union"} uses the union (subjects without a given run contribute
an empty test set for that fold).}
}
\value{
A list with elements \code{method}, \code{axis="observation"}, \code{folds},
\code{n_folds}, and \code{fold_ids}. When \code{obs_ids} is a list, each fold contains a
named list of per-subject \code{train_idx}/\code{test_idx} vectors.
}
\description{
Construct cross-validation folds along the observation axis (e.g., runs or
contiguous time blocks). Unlike subject-axis CV, these folds return per-subject
index sets suitable for timepoint/run-based cross-fitting workflows.
}
\examples{
# Leave-one-run-out folds for a single shared run vector
runs <- rep(c("run1", "run2", "run3"), each = 2)
obs_spec <- create_obs_folds(runs, method = "run")
names(obs_spec$folds)

}
\seealso{
Other cross_validation:
\code{\link{create_cv_folds}()}
}
\concept{cross_validation}
