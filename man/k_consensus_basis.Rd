% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/k_procrustes.R
\name{k_consensus_basis}
\alias{k_consensus_basis}
\title{Consensus K-orthonormal Basis (K-Procrustes Mean)}
\usage{
k_consensus_basis(
  U_list,
  K,
  weights = NULL,
  Kroots = NULL,
  max_iter = 50,
  tol = 1e-06,
  allow_reflection = TRUE,
  reflection = NULL
)
}
\arguments{
\item{U_list}{List of q x r K-orthonormal bases.}

\item{K}{Square kernel matrix.}

\item{weights}{Optional numeric weights (default equal).}

\item{Kroots}{Optional precomputed kernel roots.}

\item{max_iter}{Maximum iterations.}

\item{tol}{Convergence tolerance on \verb{1 - mean(principal cosines)}.}

\item{allow_reflection}{Passed to alignment step.}

\item{reflection}{Alias for \code{allow_reflection}.}
}
\value{
A list with elements \code{U}, \code{iters}, \code{converged}, \code{gaps}, \code{scores}.
}
\description{
Iteratively aligns bases to a current estimate, averages (optionally
weighted), and retracts back to the Stiefel manifold via K-orthonormalization.
}
\examples{
set.seed(1)
q <- 5
r <- 2
K <- diag(q)
U_list <- lapply(1:3, function(i) qr.Q(qr(matrix(rnorm(q * r), q, r))))
cons <- k_consensus_basis(U_list, K, max_iter = 5)
dim(cons$U)
}
\seealso{
Other k_procrustes:
\code{\link{k_align_bases}()},
\code{\link{k_kernel_roots}()},
\code{\link{k_orthonormalize}()},
\code{\link{k_procrustes}()}
}
\concept{k_procrustes}
