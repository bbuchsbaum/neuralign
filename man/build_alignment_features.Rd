% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feature_blocks.R
\name{build_alignment_features}
\alias{build_alignment_features}
\title{Build Alignment Matrices from Feature Blocks}
\usage{
build_alignment_features(
  blocks_by_subject,
  harmonize = c("intersection", "union_fill"),
  min_features = 2L,
  block_weights = NULL,
  fill = 0,
  union_order = c("first_seen", "sorted"),
  warn_sparse_below = 0.5,
  emit_warnings = TRUE
)
}
\arguments{
\item{blocks_by_subject}{Named list keyed by subject, where each entry is a
named list of \code{"alignment_feature_block"} objects.}

\item{harmonize}{Harmonization strategy for feature names within each block:
\code{"intersection"} keeps only features common to all subjects (via
\code{harmonize_feature_blocks()}); \code{"union_fill"} harmonizes to the union of
feature names within each block, filling missing rows with \code{fill}.
Blocks missing from any subject are dropped in both modes.}

\item{min_features}{Minimum number of features required to keep a block. For
\code{"intersection"}, this is the size of the common intersection. For
\code{"union_fill"}, blocks are dropped when any subject contributes fewer than
\code{min_features} observed features.}

\item{block_weights}{Optional named numeric vector of additional multipliers
per block name (as in \code{stack_feature_blocks()}).}

\item{fill}{Fill value used for \code{"union_fill"} (default 0).}

\item{union_order}{Ordering for union construction when \code{harmonize="union_fill"}
and no explicit union is provided: \code{"first_seen"} or \code{"sorted"}.}

\item{warn_sparse_below}{Warn when a subject observes fewer than this
fraction of union features within a block (only used for \code{"union_fill"}).}

\item{emit_warnings}{Logical; if FALSE, suppress warnings emitted during
harmonization while still returning them in the output.}
}
\value{
A list with elements:
\describe{
\item{matrices}{Named list of per-subject stacked matrices.}
\item{blocks}{Harmonized per-subject block lists used to build matrices.}
\item{per_block}{Data frame summarizing retained blocks and feature counts.}
\item{dropped_blocks}{Character vector of dropped block names.}
\item{warnings}{Character vector of warning messages encountered.}
}
}
\description{
Construct per-subject alignment matrices by harmonizing and stacking feature
blocks. This is useful for block-driven alignment workflows where different
correspondence signals (blocks) are combined into a single matrix per
subject.
}
