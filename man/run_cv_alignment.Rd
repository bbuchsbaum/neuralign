% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv.R
\name{run_cv_alignment}
\alias{run_cv_alignment}
\title{Run Cross-Validated Alignment}
\usage{
run_cv_alignment(
  data,
  method = "procrustes",
  cv_folds = "loso",
  k = 5,
  reference = "medoid",
  obs_labels = NULL,
  ...
)
}
\arguments{
\item{data}{AlignmentData object.}

\item{method}{Alignment method.}

\item{cv_folds}{CV fold information from create_cv_folds, or a string
("loso", "kfold") to create folds automatically.}

\item{k}{Number of folds if cv_folds is "kfold".}

\item{reference}{Reference selection method.}

\item{obs_labels}{Optional shared observation labels; passed through to
\code{\link{as_alignment_data}} when \code{data} is not already an
\code{\link{AlignmentData}}.}

\item{...}{Additional arguments passed to fit_alignment.}
}
\value{
List with:
\itemize{
\item result - Combined AlignmentResult with all CV transforms
\item fold_results - Deprecated; always NULL.
\item cv_info - CV configuration info
}
}
\description{
Convenience function to run alignment with cross-validation and
collect per-fold results.
}
\details{
Note: when \code{reference} is a fixed subject ID, that subject must be available
to define the anchor space. If the provided folds would otherwise place the
reference subject in a test set (e.g., LOSO), \code{run_cv_alignment()} will keep
the reference subject in the training set and will not evaluate it as a
held-out subject (a warning is emitted). Use a data-driven reference (e.g.,
\code{"medoid"}) or custom folds if you need symmetric hold-out across all
subjects.
}
