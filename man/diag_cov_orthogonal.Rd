% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uncertainty.R
\name{diag_cov_orthogonal}
\alias{diag_cov_orthogonal}
\title{Diagonal Covariance Propagation Under an Orthogonal Transform}
\usage{
diag_cov_orthogonal(variances, Q, convention = c("left", "right"))
}
\arguments{
\item{variances}{Numeric vector of length \code{d} (or \verb{d x k} matrix) giving the
diagonal variances in the source basis.}

\item{Q}{Square \verb{d x d} matrix/Matrix operator, or a list of such operators.}

\item{convention}{Convention for how \code{Q} is applied:
\code{"left"} computes \code{diag(Q \%*\% diag(v) \%*\% t(Q))};
\code{"right"} computes \code{diag(t(Q) \%*\% diag(v) \%*\% Q)}.}
}
\value{
If \code{Q} is a single matrix, returns a numeric vector (or \verb{d x k}
matrix if \code{variances} is a matrix). If \code{Q} is a list and \code{variances} is a
vector, returns a \verb{d x length(Q)} matrix whose columns correspond to each
transform.
}
\description{
Compute the diagonal of a covariance matrix after applying a (typically
orthogonal) change of basis.
}
\details{
If \code{x} has diagonal covariance \code{diag(variances)} and \code{y = Q \%*\% x}, then:
\deqn{\mathrm{diag}(\mathrm{Cov}(y)) = \mathrm{diag}(Q \, \mathrm{diag}(v) \, Q^\top).}
This can be computed without forming the full covariance via element-wise
squares:
\deqn{\mathrm{diag}(Q \, \mathrm{diag}(v) \, Q^\top) = (Q \odot Q)\, v.}

\code{convention="right"} computes the analogous quantity for \code{y = t(Q) \%*\% x}
(equivalently, for right-multiplying row vectors by \code{Q}).
}
\examples{
v <- c(a = 1, b = 2)
Q <- matrix(c(0, 1, -1, 0), 2, 2)  # 90-degree rotation (orthogonal)
diag_cov_orthogonal(v, Q, convention = "left")

}
