% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/uncertainty.R
\name{diag_cov_orthogonal_chain}
\alias{diag_cov_orthogonal_chain}
\title{Diagonal Variance Propagation Through a Transform Chain}
\usage{
diag_cov_orthogonal_chain(
  variances,
  transform,
  convention = c("left", "right"),
  subject = NULL
)
}
\arguments{
\item{variances}{Numeric vector of length \code{d} giving diagonal variances in the
source basis.}

\item{transform}{A transform specification:
\itemize{
\item A single square matrix/Matrix \code{Q}.
\item A list of square matrix/Matrix operators, applied in order
(first element applied first).
\item An \link{AlignmentModel} (or \link{AlignmentResult}) containing per-subject
operator transforms.
\item A list of \link{AlignmentModel} (or \link{AlignmentResult}) objects,
interpreted as a transform chain to apply in order (first model applied
first). Subjects are matched by ID across models.
}}

\item{convention}{Convention for how \code{Q} is applied at each step:
\code{"left"} computes \code{diag(Q \%*\% diag(v) \%*\% t(Q))};
\code{"right"} computes \code{diag(t(Q) \%*\% diag(v) \%*\% Q)}.}

\item{subject}{Optional subject id(s)/index when \code{transform} is an
\link{AlignmentModel} or list of models. If \code{NULL}, returns results for all
subjects (or all subjects common to all models in the chain).}
}
\value{
If \code{transform} is a single matrix or list of matrices, returns a
numeric vector. If \code{transform} is a model (or list of models) and
\code{subject=NULL}, returns a \verb{d x n_subjects} matrix with one column per
subject; if \code{subject} selects a single subject, returns a numeric vector.
}
\description{
Convenience wrapper to propagate diagonal variances through a sequence of
transforms without materializing dense covariances.
}
\details{
This function tracks \strong{diagonal variances only}. When chaining multiple
transforms, it applies \code{diag_cov_orthogonal()} stepwise, discarding any
off-diagonal covariances induced by intermediate transforms. This is a useful
approximation for uncertainty summaries but is not, in general, equivalent to
computing the diagonal of the fully composed covariance.
}
