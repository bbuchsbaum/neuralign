% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/block_alignment_report.R
\name{block_alignment_report}
\alias{block_alignment_report}
\title{Block-driven Alignment Diagnostics Report}
\usage{
block_alignment_report(
  blocks_by_subject,
  aligned = NULL,
  original = NULL,
  reference = NULL,
  quality_metrics = c("correlation", "reconstruction"),
  per_block_quality = FALSE,
  ...
)
}
\arguments{
\item{blocks_by_subject}{Named list keyed by subject, where each entry is a
named list of \code{"alignment_feature_block"} objects.}

\item{aligned}{Optional. Either an \code{AlignmentResult} or a named list of
aligned matrices (as accepted by \code{alignment_quality()}).}

\item{original}{Optional \code{AlignmentData} (or coercible) passed to
\code{alignment_quality()} for improvement metrics.}

\item{reference}{Optional reference matrix used for reconstruction metrics
when \code{aligned} is a plain list of matrices.}

\item{quality_metrics}{Character vector of \code{alignment_quality()} metrics to
compute when \code{aligned} is provided.}

\item{per_block_quality}{Logical; if TRUE, compute per-block reconstruction
metrics by slicing aligned and reference matrices using
\code{features$block_row_ranges}. Requires \code{aligned} and a matrix \code{reference}.}

\item{...}{Passed to \code{build_alignment_features()}.}
}
\value{
An object of class \code{"block_alignment_report"} with components:
\describe{
\item{features}{Output of \code{build_alignment_features()}.}
\item{diagnostics}{Output of \code{feature_block_diagnostics()} on harmonized blocks.}
\item{quality}{Output of \code{alignment_quality()} (or NULL if \code{aligned} is NULL).}
\item{per_block_quality}{Optional per-block reconstruction summaries (or NULL).}
}
}
\description{
Produce a unified diagnostics report for block-driven alignment workflows.
This combines:
\itemize{
\item feature-name harmonization coverage (per block, per subject),
\item rank/effective-rank diagnostics for each block and the stacked matrix,
\item optional alignment quality metrics for aligned outputs.
}
}
\details{
The report is domain-agnostic: it treats blocks as arbitrary named feature
matrices with metadata, and aligned matrices as generic numeric matrices.
}
