% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/k_procrustes.R
\name{k_procrustes}
\alias{k_procrustes}
\title{K-metric Procrustes Utilities}
\usage{
k_procrustes(Uref, U, K, allow_reflection = TRUE, reflection = NULL)
}
\arguments{
\item{Uref}{Reference basis (q x r).}

\item{U}{Basis to align (q x r).}

\item{K}{Square kernel matrix (q x q).}

\item{allow_reflection}{Logical; if \code{FALSE}, forces \code{det(R) = +1}.}

\item{reflection}{Alias for \code{allow_reflection}.}
}
\value{
A list with elements:
\describe{
\item{U_aligned}{Aligned basis \code{U \%*\% R}.}
\item{R}{Orthogonal r x r rotation/reflection matrix.}
\item{d}{Sum of singular values of the cross-gram matrix.}
\item{cosines}{Principal cosines (clipped to \verb{[0,1]}).}
\item{determinant}{\code{det(R)}.}
}
}
\description{
Utilities for K-metric (kernel-weighted) orthonormalization and Procrustes
alignment. These are general-purpose helpers for aligners that operate in a
metric defined by a positive (semi-)definite kernel \code{K}.

Align basis \code{U} to reference \code{Uref} in a kernel-weighted metric by solving
an orthogonal Procrustes problem:
\deqn{U R \approx U_{ref}}
where orthogonality is Euclidean in the reduced (column) space and the match
is measured in the \code{K}-metric.
}
\examples{
set.seed(1)
q <- 6; r <- 3
A <- matrix(rnorm(q * q), q, q)
K <- t(A) \%*\% A + diag(0.1, q)
Uref <- k_orthonormalize(matrix(rnorm(q * r), q, r), K)
Rtrue <- qr.Q(qr(matrix(rnorm(r * r), r, r)))
U <- Uref \%*\% Rtrue
pr <- k_procrustes(Uref, U, K)
norm(pr$U_aligned - Uref, "F")

}
\seealso{
Other k_procrustes:
\code{\link{k_align_bases}()},
\code{\link{k_consensus_basis}()},
\code{\link{k_kernel_roots}()},
\code{\link{k_orthonormalize}()}
}
\concept{k_procrustes}
