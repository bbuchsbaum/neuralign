% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_alignment.R
\name{apply_alignment}
\alias{apply_alignment}
\title{Apply an Alignment Model to New Data}
\usage{
apply_alignment(model, new_data, fit_new = TRUE, warn_leakage = TRUE, ...)
}
\arguments{
\item{model}{An AlignmentModel object from \code{\link{fit_alignment}},
or an AlignmentResult from which the model will be extracted.}

\item{new_data}{AlignmentData object, or a named list of matrices.
For subjects already in the model, their existing transforms are used.
For new subjects, transforms are computed using the model's reference.}

\item{fit_new}{Logical; if TRUE (default), fit transforms for subjects
not in the model. If FALSE, only apply to subjects already in model.}

\item{warn_leakage}{Logical; if TRUE (default), warn when applying to
subjects that were used in training.}

\item{...}{Additional arguments passed to the aligner's apply function.}
}
\value{
An AlignmentResult object with aligned data for the new subjects.
}
\description{
Apply a previously fitted alignment model to new data. This enables
out-of-sample alignment for cross-validation or applying trained
alignments to new subjects.
}
\examples{
# Fit on training subjects
set.seed(1)
train_data <- AlignmentData(list(
  "sub-01" = matrix(rnorm(30), 6, 5),
  "sub-02" = matrix(rnorm(30), 6, 5)
))
result <- fit_alignment(train_data, method = "procrustes")

# Apply to new subject
new_data <- AlignmentData(list(
  "sub-03" = matrix(rnorm(30), 6, 5)
))
new_result <- apply_alignment(result, new_data)

}
\seealso{
\code{\link{fit_alignment}}, \code{\link{AlignmentModel}}

Other alignment_workflow:
\code{\link{fit_alignment}()}
}
\concept{alignment_workflow}
