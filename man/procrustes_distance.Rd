% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aligner_procrustes.R
\name{procrustes_distance}
\alias{procrustes_distance}
\title{Procrustes Distance}
\usage{
procrustes_distance(
  x,
  y,
  convention = c("left", "right"),
  obs_labels_x = NULL,
  obs_labels_y = NULL,
  min_overlap = 2L,
  scale = FALSE,
  reflection = FALSE,
  allow_reflection = NULL,
  rank = NULL
)
}
\arguments{
\item{x}{First matrix.}

\item{y}{Second matrix.}

\item{convention}{Multiplication convention (\code{"left"} or \code{"right"}).}

\item{obs_labels_x}{Optional observation labels for \code{x}.}

\item{obs_labels_y}{Optional observation labels for \code{y}.}

\item{min_overlap}{Minimum number of shared labels when labels are supplied.}

\item{scale}{Logical; if \code{TRUE}, include an optimal scale factor.}

\item{reflection}{Logical; if \code{TRUE}, allow reflections.}

\item{allow_reflection}{Alias for \code{reflection}.}

\item{rank}{Optional truncated-SVD rank forwarded to
\code{\link[=procrustes_rotation]{procrustes_rotation()}}.}
}
\value{
Numeric scalar residual.
}
\description{
Convenience wrapper returning the Procrustes residual (Frobenius norm of
the alignment error) between two matrices.
}
\examples{
set.seed(1)
X <- matrix(rnorm(15), 3, 5)
Y <- X + 0.01 * matrix(rnorm(15), 3, 5)
procrustes_distance(X, Y)
}
\seealso{
Other procrustes:
\code{\link{procrustes_rotation}()}
}
\concept{procrustes}
