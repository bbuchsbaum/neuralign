% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subspace.R
\name{subspace_coordinates}
\alias{subspace_coordinates}
\title{Subspace Coordinates}
\usage{
subspace_coordinates(x, basis, side = c("left", "right"))
}
\arguments{
\item{x}{Numeric vector or matrix.}

\item{basis}{Orthonormal basis matrix with \code{d} rows and \code{r} columns.}

\item{side}{If \code{x} is a matrix, which side carries the \code{d}-dimensional space:
\code{"left"} means \code{nrow(x) == d} and returns an r-by-\code{ncol(x)} matrix;
\code{"right"} means \code{ncol(x) == d} and returns an \code{nrow(x)}-by-r matrix.}
}
\value{
Coordinates of \code{x} in the subspace.
}
\description{
Convert vectors/matrices in the ambient space into coordinates in a subspace
basis.
}
\examples{
set.seed(1)
X <- matrix(rnorm(12), 3, 4)
sb <- identified_subspace_basis(X, convention = "right")
v <- rnorm(4)
z <- subspace_coordinates(v, sb$basis)
length(z)

}
