% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intrinsic_geometry.R
\name{laplacian_eigenmodes}
\alias{laplacian_eigenmodes}
\title{Laplacian Eigenmodes}
\usage{
laplacian_eigenmodes(
  adj,
  k = 10,
  normalized = TRUE,
  drop_zero = TRUE,
  tol = 1e-10,
  method = c("auto", "eigen", "rspectra"),
  return_values = FALSE
)
}
\arguments{
\item{adj}{Symmetric adjacency matrix (dense matrix or sparse \code{Matrix}).}

\item{k}{Number of non-trivial eigenmodes to return.}

\item{normalized}{Logical; if \code{TRUE}, use the symmetric normalized Laplacian.}

\item{drop_zero}{Logical; if \code{TRUE} (default), drop near-zero eigenmodes
(constant components) and return the next \code{k}.}

\item{tol}{Tolerance for considering an eigenvalue "zero".}

\item{method}{Eigen solver: \code{"auto"} uses \code{RSpectra} if available (for sparse),
otherwise base \code{eigen()}.}

\item{return_values}{Logical; if \code{TRUE}, return a list with eigenvalues.}
}
\value{
Matrix of size \verb{(n_nodes x k)} or a list with \verb{$vectors} and \verb{$values}.
}
\description{
Compute intrinsic geometry coordinates from a symmetric adjacency graph via
Laplacian eigenmodes.
}
\examples{
# Path graph with 6 nodes
n <- 6
A <- matrix(0, n, n)
A[cbind(1:(n - 1), 2:n)] <- 1
A <- A + t(A)

Z <- laplacian_eigenmodes(A, k = 2)
dim(Z)
}
\seealso{
Other guidance: 
\code{\link{get_guidance}()},
\code{\link{guidance_channel}()},
\code{\link{intrinsic_geometry}},
\code{\link{roi_anchor_projector}()},
\code{\link{roi_anchor_projectors}()},
\code{\link{roi_anchors}},
\code{\link{set_guidance}()}
}
\concept{guidance}
