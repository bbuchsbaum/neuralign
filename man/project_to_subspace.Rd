% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/subspace.R
\name{project_to_subspace}
\alias{project_to_subspace}
\title{Project Data Onto a Subspace}
\usage{
project_to_subspace(x, basis, side = c("left", "right"))
}
\arguments{
\item{x}{Numeric vector or matrix.}

\item{basis}{Orthonormal basis matrix with \code{d} rows and \code{r} columns.}

\item{side}{If \code{x} is a matrix, which side carries the \code{d}-dimensional space:
\code{"left"} means \code{nrow(x) == d} and projects rows; \code{"right"} means
\code{ncol(x) == d} and projects columns.}
}
\value{
\code{x} projected onto the column span of \code{basis}, with the same shape as
the input.
}
\description{
Project Data Onto a Subspace
}
\examples{
set.seed(1)
X <- matrix(rnorm(12), 3, 4)
sb <- identified_subspace_basis(X, convention = "right")
v <- rnorm(4)
vp <- project_to_subspace(v, sb$basis)
length(vp)

}
